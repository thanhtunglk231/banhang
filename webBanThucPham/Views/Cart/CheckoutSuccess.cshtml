@{
    Layout = null;
    ViewData["Title"] = "Thanh toán thành công";
}
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"]</title>

    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />

    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />
</head>
<body>

    <!-- Nội dung trang -->
    <div class="container my-5 text-center">
        <h2 class="mb-4">Thanh toán thành công!</h2>
        <p class="lead">Cảm ơn bạn đã mua sắm tại chúng tôi. Đơn hàng của bạn đã được xác nhận và sẽ được xử lý sớm.</p>

        <div class="mt-5">
            <h5>Vui lòng chọn một trong các tùy chọn sau:</h5>
            <div class="d-flex justify-content-center mt-4">
                <a href="@Url.Action("CartView", "Cart")" class="btn btn-primary mx-2">
                    <i class="bi bi-cart-fill me-2"></i> Quay lại giỏ hàng
                </a>
                <a href="@Url.Action("Index", "Home")" class="btn btn-success mx-2">
                    <i class="bi bi-house-door-fill me-2"></i> Quay về trang chủ
                </a>
                <a href="@Url.Action("OrderHistory", "CustomAccount")" class="btn btn-info mx-2">
                    <i class="bi bi-clock-history me-2"></i> Xem lịch sử mua hàng
                </a>
            </div>
        </div>
    </div>

    <!-- Bootstrap 5 JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
